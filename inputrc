$if Bash
    # Up-down arrow keys do history search on partial line
    "\e[B": history-search-forward
    "\e[A": history-search-backward
$endif

set editing-mode vi
set keymap vi
# set completion-ignore-case on

$include /etc/inputrc
