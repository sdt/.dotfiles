$if Bash
    # BSB Ctrl-P for output of previous command
    # output of previous command (\r to expand !! under histverify)
    Control-p: " $(!!)"

    # Up-down arrow keys do history search on partial line
    "\e[B": history-search-forward
    "\e[A": history-search-backward
$endif

$include /etc/inputrc
